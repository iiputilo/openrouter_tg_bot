services:
  openrouter-bot:
    build:
      context: ./
    container_name: bot
    env_file:
      - .env
    environment:
      - APP_DATA_DIR=/data
    volumes:
      - openrouter_tg_bot_data:/data

volumes:
  openrouter_tg_bot_data: